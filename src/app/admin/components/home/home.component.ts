import {Component} from '@angular/core';

/**
 * This is the HomeComponent of
 *                             @see AdminModule
 *
 * It shows information about the logged user and about this application.
 * It provides some privileges in comparison with a simple user. It can see all users, it can update user's data etc.
 */

@Component({
  selector: 'app-admin-home-section',
  templateUrl: './home.component.html'
})
export class HomeComponent {
}
